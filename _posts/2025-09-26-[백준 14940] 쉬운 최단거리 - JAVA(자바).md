---
title: "[백준 14940] 쉬운 최단거리 - JAVA(자바)"
date: 2025-09-26
categories: [알고리즘]
tags: [알고리즘, 백준]
image:
  path: /assets/img/기본형3_java.png
---

### 문제

- [[백준] 14940](https://www.acmicpc.net/problem/14940)

![img](/assets/img/algorithm/백준14940.png)
<br /><br />

### 해결방법
- **너비우선탐색(BFS)** 활용하기
  - `map[][]`에 입력받은 값 저장
    - `0`인 경우는 방문처리
    - `2`인 경우는 시작점 좌표 저장
  - BFS로 탐색하면서 `answer[][]`에 이동거리 저장

<br /><br />

### 코드

```java
import java.util.*;
import java.lang.*;
import java.io.*;

class Main {
    static int n, m;
    static int[][] map;
    static int[][] answer;
    static boolean[][] visited;
    static int x, y;
    static int[] dx = {1, -1, 0, 0};
    static int[] dy = {0, 0, 1, -1};
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        n = Integer.parseInt(st.nextToken());
        m = Integer.parseInt(st.nextToken());
        map = new int[n][m];
        answer = new int[n][m];
        visited = new boolean[n][m];

        for (int i = 0; i < n; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < m; j++) {
                map[i][j] = Integer.parseInt(st.nextToken());
                if (map[i][j] == 2) {
                    x = i;
                    y = j;
                } else if (map[i][j] == 0) {
                    visited[i][j] = true;
                }
            }
        }

        bfs (x, y);

        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                if (!visited[i][j]) {
                    answer[i][j] = -1;
                }
                System.out.print(answer[i][j] + " ");
            }
            System.out.println();
        }
    }
    private static void bfs (int x, int y) {
        Queue<int[]> q = new LinkedList<>();
        q.add(new int[] {x, y});
        visited[x][y] = true;

        while (!q.isEmpty()) {
            int[] temp = q.poll();
            int curX = temp[0];
            int curY = temp[1];
            for (int i = 0; i < 4; i++) {
                int nx = curX + dx[i];
                int ny = curY + dy[i];
                if (nx >= 0 && nx < n && ny >= 0 && ny < m) {
                    if (!visited[nx][ny] && map[nx][ny] == 1) {
                        visited[nx][ny] = true;
                        answer[nx][ny] = answer[curX][curY] + 1;
                        q.add(new int[]{nx, ny});
                    }
                }
            }
        }
    }
}
```